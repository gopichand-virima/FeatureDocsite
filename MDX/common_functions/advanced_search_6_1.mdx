---
title: "Advanced Search"
description: "Open topic with navigation"
version: ""
module: "Common Functions"
section: "Advanced Search"
page: "Advanced Search"
breadcrumbs:
  - "Home"
  - ""
  - "Common Functions"
  - "Advanced Search"
  - "Advanced Search"
---

## Advanced Search

Use this function to search assets specifically as per defined values.

When this option displays in a window (such as Adding a New Task), click **Advanced Search.** The Advanced Search window displays.

- In  (for example, Display Name), click the drop-down list and select from the field names shown. A "field name" can be such items as Display Name, Roles, Groups, and so forth.
- In  (for example, First Name), click the drop-down list and select from the field names shown. A "field name" can be such items as Display Name, Roles, Groups, and so forth.
- For the  (for example, Contains), click the drop-down list and select from the choices shown. An "operator" can be such items as Contains, Does Not Contain,  Starts with and so forth.
- For the  (for example, Does Not Contain), click the drop-down list and select from the choices shown. An "operator" can be such items as Contains, Does Not Contain, Starts with, and so forth.
- For ** (for example, C), click the drop-down list and select the specific criteria to search for based on the field type. For example, if the *Field Type* selected is "Display Name," the  can be a portion of the possible display name, such as the letter C.
- For ** (for example, a), click the drop-down list and select the specific criteria to search for based on the field type. For example, if the *Field Type* selected is "First Name," the  can be a portion of the possible first name, such as the letter a.
- To add an additional row with a field type, operator, and value, click **AND** or **OR**. A new row is added.
- Repeat the above steps for each field.
- To remove a row, click the **X** at the end of the row.

! !

- When all selections/entries are made, click **Run**. The matching search results are displayed.
- If no results are found, a note displays in the results window.
- If there are multiple results that exceed the length of the results window.
- Select the applicable result and click **Add**.

! !

The selected items display in the applicable fields on the window from which the search request originated.
