# Timescale

Use this function to configure timescale settings.

In the main window, click **Admin > [Module] > Timescale**. The Timescale window displays.

Configure the response and resolution times based on priority levels.

When all configurations are complete, click **Save**.
