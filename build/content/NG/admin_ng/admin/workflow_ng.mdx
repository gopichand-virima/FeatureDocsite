# Workflow

Use this function to configure workflows for various modules.

In the main window, click **Admin > [Module] > Workflow**.

Configure the workflow stages and transitions.

When all configurations are complete, click **Save**.
