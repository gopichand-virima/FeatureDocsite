---
title: "SAML Configuration"
description: "Use this function to configure Security Assertion Markup Language, a standard for exchanging authentication and authorization data between parties between an identity provider and a service provider."
version: ""
module: "Admin Users"
section: "Saml Config"
page: "SAML Configuration"
breadcrumbs:
  - "Home"
  - ""
  - "Admin Users"
  - "Saml Config"
  - "SAML Configuration"
---

# SAML Configuration

Use this function to configure Security Assertion Markup Language, a standard for exchanging authentication and authorization data between parties between an identity provider and a service provider.


Configure of the custom application in the Azure AD Management Portal and user assignment must have taken place.


In the main window, select **Users > SAML Configuration**. The SAML Configuration window displays.

New SAML Configuration

From the *Select Actions* drop-down list, choose **New SAML Configuration**.

In the *Name* field, enter the name of the SAML.

For the *Signature Algorithm*, click the drop-down list and select the required signature.

Click **Save and Continue**. The SAML Configuration window displays.

Enter the required parameters.


When the **Upload Metadata** option is selected, the IDP Metadata field displays. Upload the applicable file.


When all selections/entries are made, click **Save**.

Edit SAML Configuration Delete SAML Configuration
