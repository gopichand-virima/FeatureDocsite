---
title: "Installing the Application"
description: "Run the Installer:"
version: ""
module: "Admin Discovery"
section: "Installing Application"
page: "Installing the Application"
breadcrumbs:
  - "Home"
  - ""
  - "Admin Discovery"
  - "Installing Application"
  - "Installing the Application"
---

# Installing the Application

1. **Run the Installer:**

  - Right-click the installer and select **Run as administrator**.

2. **Windows SmartScreen Warning:**

  - If prompted, select **More info**, and then select **Run anyway** to continue the installation.

![Installing%20the%20Application 1](
        param($match)
        $prefix = $match.Groups[1].Value
        $path = $match.Groups[2].Value
        $ext = $match.Groups[3].Value
        
        # Convert path
        $newPath = $path -replace '%20', '_' -replace '-', '_'
        $newPath = $newPath.ToLower()
        $newExt = $ext.ToLower()
        
        $fileReplacements++
        return "$prefix$newPath$newExt"
    )

1. Click **Next** to proceed.

![Installing%20the%20Application 2](
        param($match)
        $prefix = $match.Groups[1].Value
        $path = $match.Groups[2].Value
        $ext = $match.Groups[3].Value
        
        # Convert path
        $newPath = $path -replace '%20', '_' -replace '-', '_'
        $newPath = $newPath.ToLower()
        $newExt = $ext.ToLower()
        
        $fileReplacements++
        return "$prefix$newPath$newExt"
    )

1. Click **Install** to begin installation.

![Installing%20the%20Application 3](
        param($match)
        $prefix = $match.Groups[1].Value
        $path = $match.Groups[2].Value
        $ext = $match.Groups[3].Value
        
        # Convert path
        $newPath = $path -replace '%20', '_' -replace '-', '_'
        $newPath = $newPath.ToLower()
        $newExt = $ext.ToLower()
        
        $fileReplacements++
        return "$prefix$newPath$newExt"
    )

1. Click **Skip** if prompted.

![Installing%20the%20Application 4](
        param($match)
        $prefix = $match.Groups[1].Value
        $path = $match.Groups[2].Value
        $ext = $match.Groups[3].Value
        
        # Convert path
        $newPath = $path -replace '%20', '_' -replace '-', '_'
        $newPath = $newPath.ToLower()
        $newExt = $ext.ToLower()
        
        $fileReplacements++
        return "$prefix$newPath$newExt"
    )

1. Click **Finish** to complete the installation.

![Installing%20the%20Application 5](
        param($match)
        $prefix = $match.Groups[1].Value
        $path = $match.Groups[2].Value
        $ext = $match.Groups[3].Value
        
        # Convert path
        $newPath = $path -replace '%20', '_' -replace '-', '_'
        $newPath = $newPath.ToLower()
        $newExt = $ext.ToLower()
        
        $fileReplacements++
        return "$prefix$newPath$newExt"
    )

1. **Launch the Application:**

  - Find the **Virima Discovery Application** icon on your desktop.
  - Right-click and select **Run as administrator**.

![Installing%20the%20Application 6](
        param($match)
        $prefix = $match.Groups[1].Value
        $path = $match.Groups[2].Value
        $ext = $match.Groups[3].Value
        
        # Convert path
        $newPath = $path -replace '%20', '_' -replace '-', '_'
        $newPath = $newPath.ToLower()
        $newExt = $ext.ToLower()
        
        $fileReplacements++
        return "$prefix$newPath$newExt"
    )
